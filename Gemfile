source 'https://rubygems.org'

gem 'rake'
gem 'mocha'
gem 'rack-test'
gem 'test-unit'
gem 'modulla'

gem 'yard'

if RUBY_VERSION.split(?.).slice(0, 2).join(?.) <= '2.1'
  gem 'rack', '~> 1.6.0'
else
  gem 'rack'
end

gemspec
